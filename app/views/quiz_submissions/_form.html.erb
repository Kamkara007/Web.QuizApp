<%= form_with(model: quiz_submission) do |form| %>
  <% if quiz_submission.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quiz_submission.errors.count, "error") %> prohibited this quiz_submission from being saved:</h2>

      <ul>
        <% quiz_submission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_answers, style: "display: block" %>
    <%= form.text_field :user_answers %>
  </div>

  <div>
    <%= form.label :score, style: "display: block" %>
    <%= form.text_field :score %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :quiz_id, style: "display: block" %>
    <%= form.text_field :quiz_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
