<main class="container">
  <%= link_to sanitize("&larr; Back to quote"), material_path(@material) %>

  <div class="header">
    <h1>Edit quote</h1>
  </div>

  <%= turbo_frame_tag @material do %>
    <%= render "form", material: @material %>
  <% end %>
</main>